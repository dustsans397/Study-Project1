using UnityEngine;

namespace Resources
{
    [CreateAssetMenu(fileName = "ScripTable", menuName = "ScripMenu")]
    public class ScripTable : ScriptableObject
    {
        // Start is called before the first frame update
    
    }
}
